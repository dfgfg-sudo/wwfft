# Rescene v0.1.3-alpha.3 — 指定模型真实锁定版

## 修复

- 公司页选择特定模型后，不再被后台轮询立即改回“自动切换”。
- 模型下拉框优先显示用户已经接入的自定义模型，DeepSeek 官方 API 不再被免费模型长列表挤掉。
- 下达指令会把所选模型传入公司交付引擎；指定模型负责生成立项策划，失败时明确报错，不偷偷切换其他模型。
- Agent OS 能复用 Rescene 前端保存的 DeepSeek API 配置，无需重复填写 Key。
- 修复自动更新下载 404：客户端统一读取官网热更新 ZIP，下载、校验、解压并在退出后安全替换程序，随后自动重启。
- 更新失败会显示真实错误；“重试下载”会重新下载，不再误触发安装流程。
- 项目列表不再硬编码开发机上的 `re0`、`main-frontend`；创建或选择项目后会立即出现在侧栏，并正确关联当前会话。

## 下载与更新

- GitHub Release 仅发布源码与更新说明，不公开上传安装包。
- Windows ZIP 与独立 Setup 均通过 Rescene 官网邀请码页面分发。
- 已安装用户可在应用内直接热更新，无需重新下载安装包。

## 版本

- `v0.1.3-alpha.3`
- Wails 桌面版：`main-backend/build/bin/rescene.exe`
